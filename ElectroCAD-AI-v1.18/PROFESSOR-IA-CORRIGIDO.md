# Professor ElectroCAD — correção v1.11.1

A tela do Professor agora:
- importa corretamente a configuração da IA;
- verifica a disponibilidade no backend;
- não exige a API key no navegador do assinante;
- bloqueia apenas quando o administrador ainda não configurou a OpenAI;
- mostra erros da API de forma clara.

A configuração é centralizada no backend. O assinante nunca recebe a chave.
